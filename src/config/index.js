import routes from './routes';
import { API_URL, ROLES } from './contans';
import homeConfig from './homeConfig.json';

const config = {
    routes,
    API_URL,
    ROLES,
};
export { routes, API_URL, ROLES, homeConfig };
export default config;
